listener {
    timeout = 300
    on-timeout = hyprlock
}

listener {
    timeout = 900  # 15 min instead of 10
    on-timeout = loginctl lock-session
}

listener {
    timeout = 1200  # 20 min
    on-timeout = systemctl suspend
}
